<%- include("partials/header") %>

	
	 <header class="jumbotron">
         <div class="container text-center">
             <h1><img src="https://img.icons8.com/ios-filled/100/000000/fire-truck.png"/><span>  FireFighters</span></h1>
             <p>FireFighters save the world</p> 
             <p> <% if( currentUser && currentUser.isAdmin){%>
				<a class="btn btn-primary btn-lg" href="/sensors/new">Add New Sensors</a>
				 <%}%>
             </p>
            
         </div>
     </header>

		
		<div class="row text-center" id="campground-grid">
			<% sensors.forEach(function(sensor){%>
				 <div class="col-lg-4 col-md-6 col-sm-6 mb-4 justify-content-center">
					<div class="card">
						 <div class="card-body" >
							<h4 class="card-title"> Sensor 
								<%= sensor.sensorId%>
								<p><a href="/sensors/<%= sensor._id %>" class="btn btn-danger"><%= sensor.values.pop().value%>°C</a>
								</p>
								
									<span>
										<%= sensor.location %>
									</span>
							</h4>
							<a href="/sensors/<%= sensor._id %>" class="btn btn-primary">More Info</a>
						</div>
					</div>

				</div>

				<% }); %>
		</div>
	
<script>	
	function timedRefresh(time) {
	setTimeout("location.reload(true);", time);}	
	timedRefresh(30000);
</script>

	<%- include("partials/footer") %>